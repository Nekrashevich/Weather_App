import React from 'react';

/*export default (props) => {
  return <div>
    {(props.code[0] == '0') ? <WeatherDisplay zip={props.zip} lon={props.lon} lat={props.lat} /> : ''}
    {(props.code[0] == '1') ? <ForecastDisplay zip={props.zip} lon={props.lon} lat={props.lat} /> : ''}
  </div>
};  */